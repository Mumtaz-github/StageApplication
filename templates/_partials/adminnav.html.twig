{# <aside class="col-3 border border-light bg-white p-2">
	{% if is_granted('ROLE_ADMIN') or is_granted('ROLE_COMMERCIAL') %}
		<article class="card mb-3">
			<div class="card-header">
				catégorie
			</div>
			<div class="card-body">
				<p>
					<a href="{{ path('admin_categories_index') }}">Liste des catégories</a>
				</p>
				<p>
					<a href="">Ajouter une catégorie</a>
				</p>
			</div>
		</article>
		<article class="card mb-3">
			<div class="card-header">
				Produits
			</div>
			<div class="card-body">
				<p>
					<a href="{{ path('admin_produits_index') }}">Liste des produits</a>
				</p>
				<p>
					<a href="{{ path('admin_produits_app_products_new') }}">Ajouter un produit</a>
				</p>
			</div>
		</article>
	{% endif %}
	{% if is_granted('ROLE_ADMIN') or is_granted('ROLE_CLIENT_PARTICULIER') or is_granted('ROLE_CLIENT_PROFESSIONEL') %}
		<article class="card mb-3">
			<div class="card-header">
				Utilisateurs
			</div>
			<div class="card-body">
				<p>
					<a href="{{ path('app_users_index') }}">Liste des utilisateurs</a>
				</p>
			</div>
		</article>
	{% endif %}
</aside> #}

