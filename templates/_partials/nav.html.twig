<nav class="navbar navbar-expand-lg" style="background-color: re;">
	<div class="container-fluid">
		{# <a class="navbar-brand" href="{{ path('app_planning') }}">Planning App</a> #}

		<!-- Mobile Toggler -->
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-expanded="false"> <span class="navbar-toggler-icon"></span>
		</button>

		<!-- Navbar Content -->
		<div class="collapse navbar-collapse" id="navbarContent">
			<ul
				class="navbar-nav me-auto mb-2 mb-lg-0">
				<!-- Common Links -->
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_planning') }}">🗓️ Planning</a>
				</li>

				<!-- Admin Links -->
				{% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_invitation_new') }}">👤 Invitations</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_formation_index') }}">🎓 Formations</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_jour_ferie_index') }}">🌴 Jours Fériés</a>
					</li>
				{% endif %}
			</ul>

			<!-- Right Side - User Section -->
			<ul class="navbar-nav ms-auto">
				{% if app.user %}
					<!-- User Dropdown -->
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							👤
							{{ app.user.prenom }}
						</a>
						<ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
							<li>
								<a class="dropdown-item" href="{{ path('app_profile') }}">Mon Profil</a>
							</li>
							<li><hr class="dropdown-divider"></li>
							<li>
								<a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
									<i class="fas fa-power-off me-2"></i>
									Déconnexion
								</a>
							</li>
						</ul>
					</li>

					<!-- Always visible logout -->
					<li class="nav-item d-block d-lg-none">
						<a class="nav-link text-danger" href="{{ path('app_logout') }}">
							<i class="fas fa-power-off me-2"></i>
							Déconnexion
						</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_login') }}">🔑 Connexion</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
