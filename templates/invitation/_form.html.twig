{% block body %}

{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}

<div class="container mt-5 mb-0 p-4 rounded shadow-sm" style="max-width: 700px; height:450px;
 background-color:rgb(53, 67, 70) ; border-radius: 10px;">
 	<h2 class="text-center mb-4 text-white">{{ button_label|default('Créer une invitation') }}</h2>

    <div class="mb-5 mt-5">
        {{ form_label(form.email, 'Email', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.email) }}
    </div>

    <div class="mb-5">
        {{ form_label(form.role, 'Rôle', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.role, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.role) }}
    </div>


 <div class="text-center">
<div class="d-flex justify-content-between mt-5 mb-0 flex-wrap gap-2">

<a href="{{ path('app_invitation_index') }}" class="btn btn-outline-secondary px-4 mt-4">
← Retour à la liste
</a>
<button class="btn btn-outline-success px-5 py-2 mt-4">🔁
{{ button_label|default('Enregistrer') }}
</button>
{{ form_end(form) }}

{% if invitation is defined and invitation.id is not null %}
{{ include('invitation/_delete_form.html.twig',{invitation:invitation}) }}
{% endif %}
</div>
</div>

 {% endblock %} 

 {# <div class="mt-3 text-end">
    <button class="btn btn-primary">
        {{ button_label|default('Save') }}
    </button>
</div>

{{ form_end(form) }}

{% endblock %}   #}













