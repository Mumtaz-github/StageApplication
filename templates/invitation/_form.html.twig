{% block body %}

{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}

<div class="container mt-3 mb-3" style="max-width: 800px; margin: 0 auto; padding: 20px; background-color: ; border-radius: 10px; box-shadow: 0 4px 8px rgba(146, 85, 62, 0.3);">

    <div class="mb-3">
        {{ form_label(form.email, 'Email', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.email) }}
    </div>

    <div class="mb-3">
        {{ form_label(form.role, 'Rôle', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.role, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.role) }}
    </div>

</div>

<div class="mt-3 text-end">
    <button class="btn btn-primary">
        {{ button_label|default('Save') }}
    </button>
</div>

{{ form_end(form) }}

{% endblock %}













{# {% block body %}

{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}
<div class="container mt-3 mb-3" style="max-width: 800px; margin: 0 auto; padding: 20px; background -color:; border-radious: 10px; box-shadow  0 4px 8px rgba(146, 85, 62, 0.3);">

		<div class="mb-3">
			{{ form_label(form.email, 'Email', {'label_attr': {'class': 'form-label'}}) }}
			{{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
			{{ form_errors(form.email) }}
		</div>
</div>
<div class="row g-2" style="height: 120px;">
	{{ form_widget(form, {'attr': {'class': 'form-control'}}) }}
</div>
<div class="mt-3 text-end">
	<button class="btn btn-primary">
		{{ button_label|default('Save') }}
	</button>
</div>
{{ form_end(form) }}

{% endblock %} #}

