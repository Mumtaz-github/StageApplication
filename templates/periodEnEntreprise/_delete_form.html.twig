<form method="post" action="{{ path('app_period_en_entreprise_delete', {'id': period_en_entreprise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ period_en_entreprise.id) }}">
    <button class="btn btn-outline-danger px-4 py-2 mt-4 h-10">🗑️ Supprimer</button>
</form>
