{% block body %}
{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}

<div class="container mt-5 mb-0 p-4 rounded shadow-sm" style="max-width: 900px; height:570px; background-color:rgb(53, 67, 70);">
	<h2 class="text-center mb-4 text-white">{{ button_label|default('Créer une période en entreprise') }}</h2>

	<div class="mb-3">
		{{ form_label(form.dateDebut, 'Date de début', {'label_attr': {'class': 'form-label text-white'}}) }}
		{{ form_widget(form.dateDebut, {'attr': {'class': 'form-control'}}) }}
		{{ form_errors(form.dateDebut) }}
	</div>

	<div class="mb-3">
		{{ form_label(form.dateFin, 'Date de fin', {'label_attr': {'class': 'form-label text-white'}}) }}
		{{ form_widget(form.dateFin, {'attr': {'class': 'form-control'}}) }}
		{{ form_errors(form.dateFin) }}
	</div>

	<div class="mb-3">
		{{ form_label(form.formation, 'Formation associée', {'label_attr': {'class': 'form-label text-white'}}) }}
		{{ form_widget(form.formation, {'attr': {'class': 'form-select'}}) }}
		{{ form_errors(form.formation) }}
	</div>

	<div class="mb-3">
		{{ form_label(form.numbreHeures, 'Nombre d\'heures', {'label_attr': {'class': 'form-label text-white'}}) }}
		{{ form_widget(form.numbreHeures, {'attr': {'class': 'form-control'}}) }}
		{{ form_errors(form.numbreHeures) }}
	</div>



            {# <div class="d-flex justify-content-between p-3">
             <a href="{{ path('app_period_en_entreprise_index') }}" class="btn btn-outline-success">	← Retour à la liste</a>
	 <button class="btn btn-success px-5 py-2 mt-0">
				{{ button_label|default('Enregistrer') }}
			</button>  #}
     

     
<div class="text-center">
    <div class="d-flex justify-content-between mt-0 mb-0 flex-wrap gap-2">
        <a href="{{ path('app_period_en_entreprise_index') }}" class="btn btn-primary px-4 mt-4">
            ← Retour à la liste
        </a>

        <button class="btn btn-success px-5 py-2 mt-4">
            {{ button_label|default('Enregistrer') }}
        </button>
         {{ form_end(form) }} {# the form was below the first two buttion, i brought delete button from edit twig#}
      
	{# This block assumes 'formation' is passed in — only show delete if it's defined and has an ID #}
	{% if period_en_entreprise is defined and period_en_entreprise.id is not null %}

   {{ include('periodEnEntreprise/_delete_form.html.twig', {periodEnEntreprise: period_en_entreprise}) }}
   	{% endif %}

    </div>
</div>


	{# <div class="text-center">
		<div class="d-flex justify-content-between mt-0 mb-0">
			 <button class="btn btn-success px-5 py-2 mt-4">
				{{ button_label|default('Enregistrer') }}
			</button> 

			<a href="{{ path('app_period_en_entreprise_index') }}" class="btn btn-secondary px-4 mt-4">
				← Retour à la liste
			</a>
         {{ include('periodEnEntreprise/_delete_form.html.twig') }} 
		</div>#}
	 </div> 
</div> 
{% endblock %}


{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}
