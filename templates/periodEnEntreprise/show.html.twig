{% extends 'base.html.twig' %}

{% block title %}PeriodEnEntreprise{% endblock %}

{% block body %}
    <h1>PeriodEnEntreprise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ period_en_entreprise.id }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ period_en_entreprise.dateDebut ? period_en_entreprise.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ period_en_entreprise.dateFin ? period_en_entreprise.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_period_en_entreprise_index') }}">back to list</a>

    <a href="{{ path('app_period_en_entreprise_edit', {'id': period_en_entreprise.id}) }}">edit</a>

    {{ include('period_en_entreprise/_delete_form.html.twig') }}
{% endblock %}
