{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}

{# {{ form_start(form) }}
<div class="form-container">
	{{ form_row(form.actifFormation) }}
	{{ form_row(form.nom) }}
	{{ form_row(form.numero) }}
	{{ form_row(form.etat) }}
	{{ form_row(form.titreProfessionnel) }}
	{{ form_row(form.niveau) }}
	{{ form_row(form.nbStagiairesPrevisionnel) }}
	{{ form_row(form.groupeRattachement) }}
	{{ form_row(form.dateDebut) }}
	{{ form_row(form.dateFin) }}
	{{ form_row(form.formateurs) }}
</div>
<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}
{# 
{{ form_start(form) }}

<div class="container mt-3 mb-3 p-4 rounded" style="max-width: 1000px; background-color: #f1f1f1; box-shadow: 0 4px 8px rgba(0,0,0,0.1);">
    <h1 class="text-center mb-4">{{ button_label|default('Créer une Formation') }}</h1>

    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_row(form.actifFormation) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_row(form.nom) }}
        </div>

        <div class="col-md-6 mb-3">
            {{ form_row(form.numero) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_row(form.etat) }}
        </div>

        <div class="col-md-6 mb-3">
            {{ form_row(form.titreProfessionnel) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_row(form.niveau) }}
        </div>

        <div class="col-md-6 mb-3">
            {{ form_row(form.nbStagiairesPrevisionnel) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_row(form.groupeRattachement) }}
        </div>

        <div class="col-md-6 mb-3">
            {{ form_row(form.dateDebut) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_row(form.dateFin) }}
        </div>

        <div class="col-md-12 mb-3">
            {{ form_row(form.formateurs) }}
        </div>
    </div>

    <div class="text-center mt-4">
        <button class="btn btn-success px-5">{{ button_label|default('Save') }}</button>
    </div>
</div>

{{ form_end(form) }} #}


{{ form_start(form) }}

<div class="container mt-3 mb-3 p-4 rounded" style="max-width: 1000px; background-color: #f1f1f1; box-shadow: 0 4px 8px rgba(0,0,0,0.1);">
    <h1 class="text-center mb-4">{{ button_label|default('Créer une Formation') }}</h1>

    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_label(form.actifFormation) }}
            {{ form_widget(form.actifFormation, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_label(form.nom) }}
            {{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_label(form.numero) }}
            {{ form_widget(form.numero, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_label(form.etat) }}
            {{ form_widget(form.etat, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_label(form.titreProfessionnel) }}
            {{ form_widget(form.titreProfessionnel, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_label(form.niveau) }}
            {{ form_widget(form.niveau, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_label(form.nbStagiairesPrevisionnel) }}
            {{ form_widget(form.nbStagiairesPrevisionnel, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_label(form.groupeRattachement) }}
            {{ form_widget(form.groupeRattachement, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_label(form.dateDebut) }}
            {{ form_widget(form.dateDebut, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_label(form.dateFin) }}
            {{ form_widget(form.dateFin, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="row">
        <div class="col-12 mb-3">
            {{ form_label(form.formateurs) }}
            {{ form_widget(form.formateurs, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="text-center mt-4">
        <button class="btn btn-success px-5">{{ button_label|default('Save') }}</button>
    </div>
</div>

{{ form_end(form) }}