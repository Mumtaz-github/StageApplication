



<form method="post" action="{{ path('app_jour_ferie_delete', {'id': jour_ferie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jour_ferie.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
